@model BlogDetailVM

@{ int counter = 1; }

@await Component.InvokeAsync("Header", Model)

<div class="blog-details-area pt-150 pb-140">
    <div class="container">
        <div class="row">
            <div class="col-md-8">
                <div class="blog-details">
                    <div class="blog-details-img">
                        <img style="width: 100%;" src="~/assets/img/blog/@Model.Blog.Image" alt="blog-details">
                    </div>
                    <div class="blog-details-content">
                        <h2>@Model.Blog.Title</h2>
                        <p>By @Model.Blog.Name  /  @Model.Blog.Date.ToString("MMMe d, yyyy") /  <i class="fa fa-comments-o"></i> 1</p>
                        @foreach (BlogDescription description in Model.Blog.BlogDescriptions)
                        {
            <p>@description.SimpleDescription</p>
                            <p class="quote">@description.QuoteDescription</p>}
                    </div>
                    <div class="reply-area">
                        <h3>LEAVE A REPLY</h3>
                        <form id="contact-form" action="http://demo.devitems.com/eduhome/mail.php" method="post">
                            <div class="row">
                                <div class="col-md-12">
                                    <p>Name</p>
                                    <input type="text" name="name" id="name">
                                </div>
                                <div class="col-md-12">
                                    <p>Email</p>
                                    <input type="text" name="email" id="email">
                                </div>
                                <div class="col-md-12">
                                    <p>Subject</p>
                                    <input type="text" name="subject" id="subject">
                                    <p>Massage</p>
                                    <textarea name="message" id="message" cols="15" rows="10"></textarea>
                                </div>
                            </div>
                            <a class="reply-btn" href="#" data-text="send"><span>send message</span></a>
                            <p class="form-messege"></p>
                        </form>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="blog-sidebar right">
                    <div class="single-blog-widget mb-47">
                        <h3>search</h3>
                        <div class="blog-search">
                            <form id="search" action="#">
                                <input type="search" placeholder="Search..." name="search" />
                                <button type="submit">
                                    <span><i class="fa fa-search"></i></span>
                                </button>
                            </form>
                        </div>
                    </div>
                    @foreach (BlogCategory category in Model.Blog.BlogCategories)
                    {
        <div class="single-blog-widget mb-47">
            <h3>categories</h3>
            <ul>
                <li><a href="#">@category.Category.Name</a></li>

            </ul>
        </div>}
                    <div class="single-blog-widget mb-47">
                        <div class="single-blog-banner">
                            <a href="blog-details.html" id="blog"><img src="~/assets/img/blog/blog-img.jpg" alt="blog"></a>
                            <h2>best<br> eductaion<br> theme</h2>
                        </div>
                    </div>
                    <div class="single-blog-widget mb-47">
                        <h3>latest post</h3>
                        @foreach (Blog blog in Model.Blogs.Take(3))
                        {
            <div class="single-post mb-30">
                <div class="single-post-img">
                    <a asp-controller="blog" asp-action="blogdetail" asp-route-id="@blog.Id">
                        <img style="width: 122px; margin-top: 10px;" src="~/assets/img/blog/@blog.Image" alt="post">
                        <div class="blog-hover">
                            <a asp-controller="blog" asp-action="blogdetail" asp-route-id="@blog.Id"><i class="fa fa-link"></i></a>
                        </div>
                    </a>
                </div>
                <div class="single-post-content">
                    <h4><a asp-controller="blog" asp-action="blogdetail" asp-route-id="@blog.Id">@blog.Title</a></h4>
                    <p>By @blog.Name  /   @blog.Date.ToString("MMMe d, yyyy")</p>
                </div>
            </div>}
                    </div>
                    <div class="single-blog-widget">
                        <h3>tags</h3>
                        <div class="single-tag">
                            @foreach (BlogTag tags in Model.Blog.BlogTags)
                            {
                                <a href="blog-details.html" class="mr-10 mb-10">@tags.Tag.Name</a>
                            }

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>